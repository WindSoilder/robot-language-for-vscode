import * as assert from 'assert';

suite("setting table populator tests", () => {
    // I think it is no need to test setting table populator functions
    // because it is not so important for now
    // and tests for testCaseParser can also test the setting table populator
});