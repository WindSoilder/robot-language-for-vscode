"use strict";
// mainly assert that models in robotframework have some property
// note that this extension don't care about test case table and variable table in suite.
suite("keyword table populator tests", () => {
    // I think it is no need to test keyword table populator functions
    // because it is not so important for now
    // and tests for testCaseParser can also test the keyword table populator
});
//# sourceMappingURL=keywordTablePopulatorTest.js.map